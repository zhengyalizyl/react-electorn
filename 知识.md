# 为什么说agent=model+harness？ harness的六大核心组件分别解决什么问题？
- langchain 官方定义：裸llm只能做单轮文本生成，agent之所以能执行复杂任务，是因为harness（驾驭层）提供了完整的软件基础设施
- 六大组件：
  - 编排循环：决定下一步在哦什么，ReAct/Ralph循环
  - 工具调用：让agent能操作真实世界（文件、api、数据库）
  - 记忆系统：工作记忆/持久记忆/技能记忆三层，跨会话不丢失
  - 上下文管理：按需组装、裁剪、压缩上下文
  - 状态持久化：长任务中断后能恢复进度
  - 安全护栏：权限控制、审批机制、危险操作拦截
 
# loop enginering的四层架构分别是多少？验证循环为什么必须有独立的checker？
- 四层架构：
  - l1 agent循环（干活用的，模型反复调用工具直到任务完成）
  - l2 验证循环（质检用的，输出按评分标准打分，不合格带反馈重试）
  - l3 事件驱动循环（后台自动触发，如pr提交自动跑测试、定时任务）
  - l4 爬坡循环（自我优化，每轮迭代积累经验，持续提升）
为什么必须独立checker：如果让编写代码的agent自己验证自己的输出，会出现“自己给自己打分”的确认偏误--agent倾向于认为自己的输出是对的，会忽略或弱化问题。独立checker（global_check_command）是一个客观的、与编写者分离的验证命令，只有checker通过才创建pr，这是maker/checker双门控的核心

