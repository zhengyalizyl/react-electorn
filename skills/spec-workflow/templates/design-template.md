# UI 定稿：[需求名称]

| | |
| --- | --- |
| 编号 | NNN-slug |
| 设计文件 | `<project>.pen` |
| 定稿日期 | YYYY-MM-DD |
| 状态 | 评审中 / 已定稿 |

> `.pen` 是加密文件，只能通过 pencil MCP 读写。本文件是设计稿的**索引与说明**，
> 不复制设计稿内容——一旦复制就会与真源脱节。

## Frame 索引

实现时按 frame id 定位，不要按名称模糊匹配。

| Frame | id | 尺寸 | 对应用户故事 |
| --- | --- | --- | --- |
| [屏幕/区块名] | `xxxxx` | 1920×1094 | P1 |

## 设计令牌

`.pen` 里 `SetVariables` 定义的变量，与代码 `@theme` 一一对应。

| `.pen` 变量 | 值 | Tailwind 令牌 | 用途 |
| --- | --- | --- | --- |
| `$ed-bg` | `#09090B` | `--color-ed-bg` | 面板底色 |

一次性色值、数据驱动色值不进这张表，实现时走 arbitrary value 或内联 `style`。

## 区块说明

按 frame 的顶层结构逐块说明，只写**看不出来的信息**——
尺寸、颜色、间距从设计稿读，不在这里重复。

### [区块名]（`frameId`）

- **布局意图**：[哪些部分定宽、哪些部分随窗口伸缩]
- **状态**：[有哪些状态，各自如何表现。例：分镜卡片有默认态与当前态]
- **交互**：[点击/悬停/滚动的预期行为。设计稿表达不了，必须在这里写]
- **数据来源**：[真实数据从哪来，设计稿里的是占位内容还是正式文案]

## 实现时必须补的内容

设计稿只表达视觉，以下由实现补齐（详见 `pencil-ui-loop`）：

- [ ] 纯图标按钮的 `aria-label`：[列出具体按钮及名称]
- [ ] 面板 landmark 名称：[列出 `aria-label` 取值]
- [ ] 可滚动区域：[哪些区块会滚动]
- [ ] 响应式：[哪些区块 `flex-1`，哪些定宽]

## 占位内容

设计稿里不是正式文案的部分，交付时要提醒用户替换。

| 位置 | 占位内容 | 说明 |
| --- | --- | --- |

## 评审记录

| 日期 | 变更 | 结论 |
| --- | --- | --- |
