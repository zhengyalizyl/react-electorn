# 规格：[需求名称]

| | |
| --- | --- |
| 编号 | NNN-slug |
| 分支 | `NNN-slug` |
| 创建日期 | YYYY-MM-DD |
| 状态 | 草稿 / 待澄清 / 已定稿 |

> 本文件只写**需求**，不写技术方案。出现框架名、库名、文件路径即为串层，挪到 `plan.md`。

## 一、背景与目标

### 项目背景
[这个需求从哪来，当前是什么状况，不做会怎样]

### 业务目标
[做成之后业务上得到什么，一到三条]

### 用户画像
[谁在用，他们的熟练度与使用场景。这决定了 AI 在模糊决策点偏向哪一侧]

### 成功指标
[可量化、且与技术实现无关。例：3 分钟出片；首次使用无需教程即可完成一次导出]

### 边界约束
[明确不做什么。范围外的事写在这里，比写在任何地方都省钱]

## 二、功能需求

### 用户故事

按优先级排列。**每个故事必须能独立开发、独立测试、独立交付**——
P1 单独做完就应当能上线并产生价值，P2/P3 是增量。切不出独立切片说明还没拆到位。

#### P1 · [故事名]

As a [角色]，I want [能力]，so that [收益]。

**为什么是 P1**：[价值排序理由]

**验收标准**

- **Given** [前置条件]
  **When** [操作路径]
  **Then** [明确的预期结果]
- **Given** ... **When** ... **Then** ...

#### P2 · [故事名]

[同上结构]

#### P3 · [故事名]

[同上结构]

### 功能需求清单

编号全文档唯一，`tasks.md` 与测试用它回指。

| 编号 | 需求 | 所属故事 |
| --- | --- | --- |
| FR-001 | 系统必须…… | P1 |
| FR-002 | 系统必须…… | P1 |
| FR-003 | 系统必须…… | P2 |

### 边界情况与异常处理

不写这一节，AI 只会给你 Happy Path。

| 场景 | 预期行为 |
| --- | --- |
| [素材时长不足 3 秒] | [降级为单镜头，提示用户] |
| [语音合成失败] | [保留字幕轨，标红该片段，允许重试] |
| [网络中断] | [本地草稿不丢，恢复后继续] |

### 数据实体

[只写概念实体与它们的关系，字段约束留给 `plan.md` 的 `data-model.md`]

- **[实体名]**：[它代表什么，与谁关联]

## 三、待澄清事项

拿不准的地方标在这里，**不要猜一个默认值然后闷头实现**。
由 `/spec.clarify` 收口，收口后把结论写回对应章节并删除该条。

- [NEEDS CLARIFICATION: 具体问题，以及需要用户在哪几个选项里选]

## 四、假设

规格不明处所采用的合理默认值，以及它依赖的既有系统或范围边界。
每条都是一个隐含风险，写出来才能被 review。

- [假设内容] —— [如果这条不成立，影响是什么]
